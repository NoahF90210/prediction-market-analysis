"""Quarantined entry point for the pre-rebuild dashboard generator.

The public portfolio dashboard is generated by ``python3 -m src.portfolio.cli``.
Legacy learned baselines and result-bearing payload generation are intentionally
absent from the default path because the historical corpus did not support those
claims reproducibly.
"""

from __future__ import annotations


def main() -> None:
    raise RuntimeError(
        "Legacy dashboard result generation is quarantined. "
        "Use `python3 -m src.portfolio.cli build-fixture` for the synthetic demo "
        "or `python3 -m src.portfolio.cli import-normalized --input <file>` for a "
        "bounded real Polymarket sample."
    )


if __name__ == "__main__":
    main()
